/**
 * Call Service Module
 * 
 * Exports call-related services:
 * - CallService: Video call operations, token generation, history tracking
 */

const CallService = require('./call.service');

module.exports = {
  CallService,
};
